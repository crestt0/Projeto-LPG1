package com.exemplo.listadeafazeresfx;

/**
 * Esta classe serve apenas como um ponto de entrada "limpo" para o JAR executável.
 * Ela não tem nenhuma herança do JavaFX, evitando conflitos no carregamento.
 */
public class Launcher {
    public static void main(String[] args) {
        // A única função desta classe é chamar o main da sua aplicação principal.
        AplicacaoPrincipal.main(args);
    }
}